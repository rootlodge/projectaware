{
  "name": "neversleep.ai",
  "version": "1.0.0",
  "description": "neversleep.ai is an experimental AI project focused on developing an autonomous agent using neural networks and large language models (LLMs). The agent first defines itself (identity, values, goals, etc.) by reasoning with multiple LLMs (gemma3, dolphin3 via Ollama). All self-knowledge is stored in memory and can be updated or expanded. After self-definition, the agent can move to more advanced tasks (evolutionary coding, problem-solving, etc.).",
  "main": "agent.js",
  "scripts": {
    "start": "node agent.js",
    "dev": "node agent.js",
    "agent": "node agent.js",
    "loop": "node loop.js",
    "setup": "npm install && echo \"Setup complete! Make sure Ollama is running with gemma3 and dolphin3 models before running 'npm start'\"",
    "check": "node -c agent.js && node -c brain.js && node -c memory.js && node -c MultiAgentManager.js && node -c InternalAgentSystem.js && echo \"All files syntax OK!\"",
    "clean-logs": "Remove-Item ./logs/*.log -ErrorAction SilentlyContinue; echo \"Logs cleaned\"",
    "clean-memory": "Remove-Item ./memory.db -ErrorAction SilentlyContinue; echo \"Memory database reset\"",
    "clean-rewards": "node cleanup-rewards.js",
    "analyze": "node -e \"const {getConversationHistory, getUserResponsePatterns} = require('./memory'); const {analyzeSatisfaction} = require('./brain'); (async()=>{const h=await getConversationHistory(20); const a=await analyzeSatisfaction(h); const p=await getUserResponsePatterns(30); console.log('SATISFACTION:',a); console.log('PATTERNS:',p);})()\"",
    "reset": "npm run clean-logs && npm run clean-memory && echo \"Full reset complete\"",
    "debug": "node --inspect agent.js",
    "test-multi-agent": "node tests/test-multi-agent.js",
    "test-internal-agents": "node tests/test-internal-agents.js",
    "test-emotion-engine": "node tests/test-emotion-engine.js",
    "test-cache-help": "node tests/test-cache-help.js",
    "test": "node tests/test-multi-agent.js && node tests/test-internal-agents.js && node tests/test-emotion-engine.js && node tests/test-cache-help.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "axios": "^1.10.0",
    "chalk": "^4.1.2",
    "crypto": "^1.0.1",
    "events": "^3.3.0",
    "fs": "^0.0.1-security",
    "fs-extra": "^11.3.0",
    "inquirer": "^12.7.0",
    "path": "^0.12.7",
    "readline": "^1.3.0",
    "sqlite3": "^5.1.7"
  }
}
